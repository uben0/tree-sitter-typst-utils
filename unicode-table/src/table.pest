file  =  { SOI ~ entry* ~ EOI            }
entry =  { hexa ~ (";" ~ field)* ~ "\n"+ }
hexa  = @{ ('0'..'9' | 'A'..'F')+        }
field = @{ (
    'a'..'z' | 'A'..'Z' |
    '0'..'9' | "_" | "-" |
    "/" | "," | " " | "<" |
    ">" | "(" | ")"
)* }
